---
import SiteMeta from "../components/SiteMeta.astro";
import { Nav } from "../components/Nav";
const { title } = Astro.props;
---

<style>
  @media (min-width: 768px) {
    .grid-template {
      grid-template: "a b" auto / 18rem auto;
    }
  }
</style>

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <SiteMeta title={title} />
  </head>
  <body>
    <div
      class="max-w-7xl mx-auto grid md:h-screen grid-template text-neutral-900"
    >
      <Nav client:load>
        <!--   <li>
          <Link href="/" class="block md:hover:opacity-75">
            Introduction
          </Link>
        </li>
        {pages &&
          pages.map(({ slug, title }) => {
            invariant(slug, "pages must have a slug");
            return (
              <li key={slug}>
                <Link href={slug} class="block md:hover:opacity-75">
                  {title}
                </Link>
              </li>
            );
          })}
           -->
      </Nav>
      <main class="overflow-auto px-6 md:px-9 py-32 md:pt-9 md:pb-20 w-full">
        <slot />
      </main>
    </div>
  </body>
</html>
